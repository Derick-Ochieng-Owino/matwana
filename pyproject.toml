[project]
name = "matwana"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "Django==5.2.6",
    "gunicorn",
    "whitenoise",
    "dj-database-url",
    "python-dotenv",
    "psycopg2-binary"
]

[tool.setuptools]
packages = []